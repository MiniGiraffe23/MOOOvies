<div class="comment-form">
<%= form_for :comment, url: movie_comments_path(@movie) do |f| %>
<p>
  <%= f.label :body %><br>
  <%= f.text_area :body %>
  <%=f.submit "submit comment"%>
</p>
<%end%>
</div>
